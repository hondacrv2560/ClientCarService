<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.client">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".Activity.ActivityCarWashing_Standart"/>
        <activity android:name=".Activity.ActivityTireFitting"/>
        <activity android:name=".Activity.ActivityFullOrderCW_Standart"/>
        <activity android:name=".Activity.ActivityFullOrderCW_3Phases"/>
        <activity android:name=".Activity.ActivityFullOrder_RepairWindshield"/>
        <activity android:name=".Activity.ActivityFullOrder_Polishing"/>
        <activity android:name=".Activity.ActivityFullOrder_Nanoceramics"/>
        <activity android:name=".Activity.ActivityClientInfo"/>
        <activity android:name=".Activity.ActivityOrdersClientView"/>
        <activity android:name=".Activity.RegularClientActivity" />
        <activity android:name=".Activity.ActivityContacts"/>
        <activity android:name=".Activity.CarWashing_3PhasesActivity" />
        <activity android:name=".Activity.RepairWindshieldActivity" />
        <activity android:name=".Activity.PolishingActivity" />
        <activity android:name=".Activity.ToningActivity" />
        <activity android:name=".Activity.NanoCeramicsActivity" />
        <activity android:name=".Activity.ProtectiveFilmActivity" />
        <activity android:name=".Activity.ChemicalCleaningSalonActivivy" />
        <activity android:name=".Activity.SalonProtectionActivity" />
        <activity android:name=".Activity.ServiceClientActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>